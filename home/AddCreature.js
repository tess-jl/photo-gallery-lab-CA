import Component from '../Component.js';

class AddCreature extends Component {

    //makes the form
    onRender() {
        

    }

    renderHTML(submit) {
        //passed the function onSubmit as a callback 
        const { onSubmit } = this.props; 
    
    }
}

export default AddCreature;